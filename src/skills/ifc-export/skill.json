{
  "slug": "ifc-export",
  "name": "IFC Export",
  "version": "1.0.0",
  "description": "Export floor plans and 3D models to IFC 2x3 format for BIM tool compatibility",
  "disciplines": ["architectural", "interior"],
  "requiredSecrets": [],
  "optional": true,
  "defaultEnabled": true,
  "agentTools": [
    {
      "name": "export_ifc",
      "description": "Export the current floor plan and 3D model to IFC format",
      "parameters": {
        "type": "object",
        "properties": {
          "include_furniture": { "type": "boolean", "description": "Whether to include furniture in the IFC export" },
          "ifc_version": { "type": "string", "enum": ["2x3", "4"], "description": "IFC schema version" }
        }
      }
    }
  ],
  "hooks": ["onFloorPlanUpdated"]
}
